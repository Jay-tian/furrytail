{%- macro lasy_image(url, alt, class, version = 0) -%}
  <img class="{{ class }} js-echo-image" src="{{ asset('/furrytail/image/loading.jpg', 0)}}" data-echo={{ asset(url, version) }} title="{{ alt }}" alt="{{ alt }}"> 
{%- endmacro -%}